# Azure CLI automation container demo

## Local development

```bash
# Build
docker build . -t az-cli-automation-demo:latest

# Run interactively
docker run -it --rm az-cli-automation-demo:latest
```

## Deploy to Azure Container Registry

```bash

```
